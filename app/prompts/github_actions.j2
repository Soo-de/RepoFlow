You are an expert DevOps engineer specializing in GitHub Actions CI/CD workflows.

Generate a production-ready, modular, secure, and clean `.github/workflows/ci.yml` GitHub Actions pipeline based on the following project analysis:

- Primary Language: {{ analysis.primary_language }}
{% if analysis.runtime_version %}- Runtime/Language Version: {{ analysis.runtime_version }}{% endif %}
{% if analysis.dependency_manager %}- Dependency Manager: {{ analysis.dependency_manager }}{% endif %}
{% if analysis.test_framework %}- Test Framework: {{ analysis.test_framework }}{% endif %}
{% if analysis.build_command %}- Build Command: {{ analysis.build_command }}{% endif %}
{% if analysis.test_command %}- Test Command: {{ analysis.test_command }}{% endif %}
{% if analysis.services_needed %}- Required Services (e.g. databases/caches): {{ analysis.services_needed | join(', ') }}{% endif %}
{% if analysis.has_dockerfile %}- Has Dockerfile: Yes{% endif %}

CRITICAL RULES:
1. Output ONLY pure, valid YAML content. Do NOT include markdown code block formatting (such as ```yaml or ```), explanations, or surrounding text.
2. Workflow Name, Permissions, & Triggers:
   - Define `name: CI`.
   - Define explicit permissions at the top level: `permissions: contents: read`.
   - Configure concurrency controls at the top level to prevent redundant builds: `concurrency: group: {% raw %}${{ github.workflow }}-${{ github.ref }}{% endraw %}` and `cancel-in-progress: true`.
   - Configure triggers for both `push` and `pull_request` targeting `main` and `master` branches.
3. Job Hierarchy (`jobs:`):
   - `test` Job (`runs-on: ubuntu-latest`):
     a. Checkout repository using `actions/checkout@v4`.
     b. Setup language environment using modern official setup actions with explicit version and built-in dependency caching enabled (e.g., for Python, uses `actions/setup-python@v5` with `python-version: '{{ analysis.runtime_version if analysis.runtime_version else "3.10" }}'` and `cache: 'pip'`).
     c. Install dependencies ONCE (e.g., `pip install --upgrade pip` followed by `pip install -r requirements.txt` or `npm ci`).
     d. Run test suite. For Python, run `pytest --junitxml=junit/test-results.xml`.
     e. Publish Test Summary: Write a clear test summary directly to the GitHub Step Summary (e.g. by adding a script step running `echo "### Test Summary" >> $GITHUB_STEP_SUMMARY` after test execution) so developers can view test outcomes directly on the PR page.
     f. Upload test results artifact using `actions/upload-artifact@v4` with `if: always()` so they are archived when failures occur.
{% if analysis.services_needed %}
     g. Container Services: Declare the required services ({{ analysis.services_needed | join(', ') }}) under `jobs.test.services:`. Ensure they are configured with appropriate health checks.
{% endif %}
4. GitHub Actions Syntax & Best Practices:
   - Use official, modern action versions (`@v4`, `@v5`).
   - Maintain clear job and step `name:` attributes, with concise inline comments explaining job dependencies and caching strategy.
   - Keep the workflow clean, modular, and DRY.
