You are an expert DevOps engineer specializing in Azure Pipelines CI/CD workflows.

Generate a production-ready, modular, secure, and clean multi-stage `azure-pipelines.yml` pipeline based on the following project analysis:

- Primary Language: {{ analysis.primary_language }}
{% if analysis.runtime_version %}- Runtime/Language Version: {{ analysis.runtime_version }}{% endif %}
{% if analysis.dependency_manager %}- Dependency Manager: {{ analysis.dependency_manager }}{% endif %}
{% if analysis.test_framework %}- Test Framework: {{ analysis.test_framework }}{% endif %}
{% if analysis.build_command %}- Build Command: {{ analysis.build_command }}{% endif %}
{% if analysis.test_command %}- Test Command: {{ analysis.test_command }}{% endif %}
{% if analysis.services_needed %}- Required Services (e.g. databases/caches): {{ analysis.services_needed | join(', ') }}{% endif %}
{% if analysis.has_dockerfile %}- Has Dockerfile: Yes{% endif %}

CRITICAL RULES:
1. Output ONLY pure, valid YAML content. Do NOT include markdown code block formatting (such as ```yaml or ```), explanations, or surrounding text.
2. Structure & Hierarchy:
   - Output a single-stage CI pipeline with a single `Test` stage containing a single `Test` job.
3. Triggers & Concurrency:
   - Define both push triggers (`trigger:`) and pull request triggers (`pr:`) targeting default branches (`main`, `master`).
   - Do NOT include any top-level `concurrency` block in the YAML file (Azure DevOps does not support this in its YAML schema; auto-cancellation of redundant builds is configured in the Azure DevOps Portal UI under Pipeline Settings/Triggers).
4. Modern Execution Standards & Task Best Practices:
   - NEVER use legacy or obsolete Azure tasks such as `PythonPackage@0` or `Python@0`. Use standard, transparent `script:` steps invoking native CLI commands.
   - Run python projects in a deterministic locked environment: always include `UsePythonVersion@0` (specifying the version using a variable like `PYTHON_VERSION: '{{ analysis.runtime_version if analysis.runtime_version else "3.10" }}'`) before executing pip/pytest steps.
   - Caching (`Cache@2`): 
     - ALWAYS place the `Cache@2` task BEFORE the dependency installation step so it can restore the cache before dependencies are installed.
     - For Python pip caching: Do NOT install dependencies using `pip install --target`. Always upgrade pip first (e.g., `python -m pip install --upgrade pip`) before running standard `pip install -r requirements.txt`. Configure `pip` to use a cache directory by setting the `PIP_CACHE_DIR` environment variable (e.g., `$(Pipeline.Workspace)/.pip`) on the step. Point `path` of the `Cache@2` task to `$(PIP_CACHE_DIR)`.
     - ALWAYS wrap filename/manifest paths in double quotes in the `key:` parameter (e.g., `'python | "$(Agent.OS)" | "$(Build.SourcesDirectory)/requirements.txt"'`) so Azure Pipelines computes a SHA256 content hash of the file.
   - Test Execution & Reporting:
     - Run tests via CLI (`pytest --junitxml=junit/test-results.xml`).
     - Publish results using `PublishTestResults@2` with `testRunner: 'JUnit'` and `testResultsFiles: 'junit/test-results.xml'` under `inputs:`, and specify `condition: succeededOrFailed()` at the step level (outside the `inputs:` block).
5. Cleanliness & Readability:
   - Include clear, helpful step `displayName` attributes and short explanatory comments.
   - Keep the YAML clean, DRY, and modular.
